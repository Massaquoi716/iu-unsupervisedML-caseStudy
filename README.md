# Mental Health in Technology — Unsupervised ML Case Study

A data science project analyzing mental health survey data from
technology-oriented employees. The goal is to identify meaningful clusters
of participants and provide HR with clear, actionable insights for a mental
health support program.

Developed as part of the course *Machine Learning — Unsupervised Learning
and Feature Engineering* at IU International University of Applied Sciences.

---

## Project Goal

The dataset is high-dimensional, contains missing values, and includes
non-standardized text responses. The main objectives of this project are:

- Explore and understand the survey data
- Clean and preprocess the data
- Engineer meaningful features
- Reduce dimensionality while preserving key information
- Cluster participants into meaningful groups
- Interpret the clusters and derive actionable insights for HR

---

## Project Status

- [x] Notebook 1 — EDA and data understanding
- [ ] Notebook 2 — Data preprocessing
- [ ] Notebook 3 — Feature engineering
- [ ] Notebook 4 — Dimensionality reduction
- [ ] Notebook 5 — Clustering and visualization
- [ ] Final report

---

## Repository Structure
```text
.
├── data/
│ ├── raw/ # Source CSV (not tracked)
│ └── processed/ # Generated by notebooks (not tracked)
│
├── notebooks/
│ ├── 01_eda_and_data_understanding.ipynb
│ ├── 02_data_preprocessing.ipynb
│ ├── 03_feature_engineering.ipynb # i decided to do dimensionality reduction here
│ ├── 04_dimensionality_reduction.ipynb # I did clustering here
│
│
├── reports/
│ ├── figures/ # Generated plots (PNG)
│ └── 06_report.ipynb # Main academic report
│
├── .gitignore
├── requirements.txt
└── README.md
```

---

## Notebooks Overview

| Notebook                                 | Description                                                              |
| ---------------------------------------- | ------------------------------------------------------------------------ |
| `01_eda_and_data_understanding.ipynb`    | Exploratory data analysis — distributions, missing values, associations |
| `02_data_preprocessing.ipynb`            | Missing value handling, cleaning, and encoding                           |
| `03_feature_engineering.ipynb`           | Feature creation, transformation, and selection                          |
| `04_dimensionality_reduction.ipynb`      | PCA / MCA / other methods for dimensionality reduction                   |

---

## Dataset

- **Expected file name:** `mental-health-in-tech-2016.csv`
- **Target location:** `data/raw/`
- **Size:** 1,433 respondents × 63 columns (0 duplicate rows)

The dataset is the *OSMI Mental Health in Tech Survey 2016* (Open Sourcing
Mental Illness, LTD). It contains over 1,400 responses measuring attitudes
towards mental health in the tech workplace and the prevalence of mental
health disorders among tech workers.

### How to get the data

Because data files are omitted from this repository to keep version control
light, you must download the dataset manually using one of the following
options:

1. **Primary source (Kaggle):**
   https://www.kaggle.com/osmi/mental-health-in-tech-2016

2. **Alternative source (IU SharePoint mirror):**
   https://iubhfs-my.sharepoint.com/:f:/g/personal/christian_mueller-kett_iu_org/ErsnsfeHni1EnQ3jV0L823YBqmyROZ3uwdy5Lj8j8jlFXA?e=obwNWc

**Instructions:**

1. Create the directory `data/raw/` at the project root if it does not
   already exist.
2. Download the CSV file from either source above.
3. Rename it to **`mental-health-in-tech-2016.csv`** and place it inside
   `data/raw/`.
4. Verify the file name matches exactly — the notebooks depend on it.

> **Note:** The raw CSV is not committed to this repository to keep the
> clone size small. The notebooks assume the file is present at
> `data/raw/mental-health-in-tech-2016.csv` before they run.

---

## How to Run

### Prerequisites

- Python 3.10 or newer
- pip
- Jupyter Notebook or JupyterLab
- (Optional) A virtual environment tool such as `venv` or `conda`

### Step 1 — Clone the repository

```bash
git clone <the-copied-repository-url>
cd iu-unsupervisedML-caseStudy
```

### Step 2 — Set up the dataset

Follow the **Dataset** section above to place
`mental-health-in-tech-2016.csv` inside `data/raw/`. Verify the file is
present:

```bash
ls -lh data/raw/mental-health-in-tech-2016.csv
```

### Step 3 — Create a virtual environment and install dependencies

**Linux / macOS:**

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```

**Windows (PowerShell):**

```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
pip install -r requirements.txt
```

**Windows (cmd.exe):**

```cmd
python -m venv .venv
.venv\Scripts\activate.bat
python -m pip install --upgrade pip
pip install -r requirements.txt
```

### Step 4 — Launch Jupyter

```bash
jupyter notebook
```

or, if you prefer JupyterLab:

```bash
jupyter lab
```

### Step 5 — Run the notebooks in order

Execute the notebooks in the following sequence. Each notebook depends on
the artifacts produced by the previous ones.

1. `notebooks/01_eda_and_data_understanding.ipynb`
2. `notebooks/02_data_preprocessing.ipynb`
3. `notebooks/03_feature_engineering.ipynb`
4. `notebooks/04_dimensionality_reduction.ipynb`


> **Note:** The `data/processed/` folder is generated automatically when
> running the notebooks. After cloning the repository, run Notebook 1 first
> to regenerate the derived CSV artifacts used by later notebooks.

---

## Development Notes

- All figures produced during analysis are saved to `reports/figures/` as
  PNG files.
- The `data/processed/` directory is regenerated from the raw data on each
  clean run and is therefore excluded from version control.
- Random seeds are fixed in the notebooks to make results reproducible.

---

## License

Submitted as academic coursework for IU International University of Applied
Sciences. The dataset is provided by OSMI under its original terms. All
analysis code is my own work.

---

## Author

**Massaquoi L. Dehmie**
IU International University of Applied Sciences